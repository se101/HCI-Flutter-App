import 'package:flutter/material.dart';

class OverWeightWorkoutScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Weekly Workout Plan'),
      ),
      body: Text('over'),
    );
  }
}
